import 'package:flutter/material.dart';

class CompressorScreen extends StatelessWidget {
  static final String id = "CompressorScreen";

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
